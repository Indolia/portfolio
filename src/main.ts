/**
 * Portfolio site entry point.
 * Initializes theme handler and any other global functionality.
 */

import { initTheme } from "./utils/themeHandler";
import { initExperienceTimeline } from "./experience";

initTheme();
initExperienceTimeline();
